% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova.R
\name{ancova_resolve_contrasts}
\alias{ancova_resolve_contrasts}
\title{Resolve the set of ANCOVA contrasts to compute}
\usage{
ancova_resolve_contrasts(group_contrasts, orig_levels, labels)
}
\arguments{
\item{group_contrasts}{Either \code{NULL} or a fully named list of length-2 character
vectors and/or numeric weight vectors. See \code{\link[=set_vars]{set_vars()}}.}

\item{orig_levels}{Character vector of the group factor levels (in order).}

\item{labels}{Character vector of \code{ref}/\code{alt}/... labels from \code{\link[=ancova_group_labels]{ancova_group_labels()}}.}
}
\value{
A list of contrast records (see description).
}
\description{
Translates the optional \code{group_contrasts} specification into a list of contrast
records. Each record holds a weight vector over the group levels (\code{weights}, in
factor-level order, summing to zero), the output \code{parameter} name, an optional
user \code{label}, and the \code{level_1} / \code{level_2} group labels for simple pairwise
contrasts (\code{NA} for general weight-vector contrasts).
}
\details{
When \code{group_contrasts} is \code{NULL} the default is each non-reference level versus the
reference level, using the backwards-compatible \code{trt} / \code{trt_alt2} / ... naming.
Otherwise the list must be fully named; each element is either a length-2 character
vector \code{c(minuend, subtrahend)} (a pairwise contrast) or a numeric weight vector over
the group levels, and the list name is used as the \code{parameter} name.
}
\keyword{internal}

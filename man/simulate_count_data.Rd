% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_count_data.R
\name{simulate_count_data}
\alias{simulate_count_data}
\title{Simulate longitudinal count data}
\usage{
simulate_count_data(
  n = 100,
  groups = c("Control", "Intervention"),
  periods = as.character(1:3),
  log_rates = NULL,
  duration = 1,
  phi = 0.5,
  baseline_effect = 0,
  prob_miss = 0
)
}
\arguments{
\item{n}{Number of subjects per treatment group. Either one positive integer
or one positive integer for each element of \code{groups}.}

\item{groups}{Character vector containing the treatment-group levels.}

\item{periods}{Character, numeric, or factor vector containing the ordered
period values.}

\item{log_rates}{Numeric matrix of marginal log event rates, with one row per
treatment group and one column per period. If \code{NULL}, all log rates are 0.}

\item{duration}{Positive numeric exposure for each period. Either a scalar or
a vector with one value per period.}

\item{phi}{Positive frailty variance. Either a scalar shared by all groups or
one value per treatment group. Conditional on its frailty, a subject's
period counts are independent Poisson variables. The frailty has mean 1
and variance \code{phi}.}

\item{baseline_effect}{Numeric coefficient for a standard-normal,
subject-level baseline covariate on the log-rate scale.}

\item{prob_miss}{Probability that the outcome is missing in each period.
Either a scalar or one value per period. Missingness is generated
independently after the complete counts have been simulated.}
}
\value{
A \code{data.frame} with one row per subject and period and columns \code{id},
\code{period}, \code{duration}, \code{group}, \code{baseline}, \code{outcome_full}, and \code{outcome}.
The \code{outcome_full} column contains the count before missingness is applied.
}
\description{
Simulates data from the gamma--Poisson model used for count outcomes in
\code{rbmi}. Each subject has a gamma-distributed frailty shared by all periods,
so the marginal period counts are negative binomial and counts from the same
subject are correlated.
}
\examples{
set.seed(123)
log_rates <- rbind(
    Control = log(c(1.0, 0.8, 0.8)),
    Intervention = log(c(1.4, 1.2, 1.2))
)
data <- simulate_count_data(
    n = 20,
    groups = rownames(log_rates),
    periods = as.character(1:3),
    log_rates = log_rates,
    duration = c(1, 1, 1),
    phi = 0.4,
    prob_miss = c(0, 0, 0.3)
)
head(data)

}

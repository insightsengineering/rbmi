% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{untranspose_imputations}
\alias{untranspose_imputations}
\title{Invert imputation list}
\usage{
untranspose_imputations(imputations, sample_ids)
}
\arguments{
\item{imputations}{A list of imputed values with 1 element per unique subject}

\item{sample_ids}{A list of ids belonging to each sample with 1 element per sample}
}
\description{
Invert imputation list
}
\details{
This function converts a list of imputations from being structured per patient
to being structured per sample i.e. it converts\preformatted{input_imputes <- list(
    list(id = "Ben", values = list(4, 2, 1),
    list(id = "Harry", values = list(c(1, 2))),
    list(id = "Phil", values = list(c(3, 4), c(5, 6))),
)

sample_ids <- list(
    c("Ben", "Harry", "Phil"),
    c("Ben", "Ben", "Phil")
)
}

to\preformatted{list(
    list(
        list(id = "Ben", values = 4),
        list(id = "Harry", values = 1),
        list(id = "Phil", values = c(3,4))
    ),
    list(
        list(id = "Ben", values = 2),
        list(id = "Ben", values = 1),
        list(id = "Phil", values  = c(5,6))
    )
)
}
}

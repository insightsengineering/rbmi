% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_time.R
\name{split_time}
\alias{split_time}
\title{Split count-data rows into time intervals}
\usage{
split_time(
  data,
  start,
  duration,
  outcome,
  event_times = NULL,
  interval,
  origin = NULL,
  period = NULL
)
}
\arguments{
\item{data}{A data frame with one row per original count period.}

\item{start}{Name of the row start variable. It must be numeric or a \code{Date}.}

\item{duration}{Name of the non-negative duration variable, measured in days
for \code{Date} starts and in the same units as a numeric \code{start}.}

\item{outcome}{Name of the aggregate count variable.}

\item{event_times}{Optional character vector naming columns that contain
one-based event-time offsets from \code{start}. These are required to allocate
a positive observed count when a row intersects more than one interval.}

\item{interval}{A positive whole number giving a fixed interval width, or
one of \code{"day"}, \code{"week"}, \code{"month"}, \code{"quarter"}, or \code{"year"}.
Calendar months, quarters, and years require \code{Date} starts.}

\item{origin}{Optional interval origin. For fixed intervals this is the
boundary immediately before interval zero. For calendar intervals it must
be the first day of a month. Defaults to \code{0} for numeric starts and
\code{1970-01-01} for \code{Date} starts. (Note that \verb{interval = "week"`` with the default numeric origin 0 / }1970-01-01`` (a Thursday) produces Thursday-based weeks.)}

\item{period}{Optional name of the original period variable. When supplied,
\code{split_period} combines this value with \code{split_interval}, which allows two
original periods intersecting the same interval to remain distinct.}
}
\value{
A data frame with the original columns and six additional columns:
\code{split_start}, \code{split_end}, \code{split_interval}, \code{split_period},
\code{split_duration}, and \code{split_outcome}. \code{split_period} is an ordered factor
suitable for \code{set_vars(period = "split_period")}.
}
\description{
\code{split_time()} divides each count-data row into fixed-width or calendar
intervals. Event-time columns are used to allocate an observed aggregate
count to the resulting cells. Missing aggregate counts remain missing in
every resulting positive-duration cell.
}
\examples{
dat <- data.frame(
    id = 1,
    period = "observed",
    start = 10,
    duration = 10,
    count = 2,
    event1 = 2,
    event2 = 9
)
split_time(
    dat,
    start = "start",
    duration = "duration",
    outcome = "count",
    event_times = c("event1", "event2"),
    interval = 7,
    origin = 0,
    period = "period"
)

}
